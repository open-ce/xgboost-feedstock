package:
  name: _xgboost_select
  version: {{ xgboost_select_version }}

build:
  number: 1
  string: {{ build_type }}_{{ PKG_BUILDNUM }}

about:
  summary : Package used to select the specific XGBoost build variant

extra:
  recipe-maintainers:
    - open-ce/open-ce-dev-team

